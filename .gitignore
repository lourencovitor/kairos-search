node_modules
.pnpm-store

# Runtime data — only exclude large intermediate files and historical runs
data/job-research/runs/
data/job-research/latest/raw-jobs.json
data/job-research/latest/rejected-jobs.json
data/manual-inputs/*.json
!data/manual-inputs/*.example.json

# Local env & secrets
.env
.env.local
.env*.local

# Tool dirs
.claude
.kiro
.playwright-cli

# Screenshots
*.png

# macOS
.DS_Store

# Build outputs (nested — root dist/ is committed intentionally)
src/job-research-agent/web/client/dist/

# Editor
.vscode
